import Navigation from '../UI/Navigation';

const Header = () => {
  return (
    <header>
      <div className='container'>
        <Navigation />
      </div>
    </header>
  );
};

export default Header;
